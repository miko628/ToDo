<Window x:Class="ToDoApp.View.TaskCreatorView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:ToDoApp.View"
        mc:Ignorable="d"
        Title="TaskCreatorView" Height="450" Width="800">
    <Grid >
        <Grid.RowDefinitions>
            <RowDefinition Height="5*"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <TextBlock Text="{Binding tekst}" Grid.Row="0" Grid.ColumnSpan="2" Background="Red" FontSize="55"/>
        <Button Grid.Row="2" Grid.Column="1" Command="{Binding SaveCommand}" Content="Zapisz" FontSize="25"/>
        <Button Grid.Row="2" Grid.Column="0" Command="{Binding CancelCommand}" Content="Anuluj" FontSize="25"/>
    </Grid>
</Window>
